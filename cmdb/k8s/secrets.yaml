# Kubernetes Secrets (Template - DO NOT commit real values)
# AI Agent Governance Framework - Internal v2.1
#
# Generate actual secrets with:
# kubectl create secret generic mongodb-secret \
#   --from-literal=username=admin \
#   --from-literal=password=<secure-password> \
#   -n cmdb
#
# kubectl create secret generic cmdb-secret \
#   --from-literal=mongodb-uri=mongodb://admin:<password>@mongodb:27017/cmdb?authSource=admin \
#   --from-literal=api-key=<secure-api-key> \
#   -n cmdb

apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: cmdb
type: Opaque
stringData:
  username: admin
  password: REPLACE_WITH_SECURE_PASSWORD
---
apiVersion: v1
kind: Secret
metadata:
  name: cmdb-secret
  namespace: cmdb
type: Opaque
stringData:
  mongodb-uri: mongodb://admin:REPLACE_WITH_SECURE_PASSWORD@mongodb:27017/cmdb?authSource=admin
  api-key: REPLACE_WITH_SECURE_API_KEY
